# Session Notes — 2026-03-15 (twinkly-frolicking-abelson)

## Issues Completed

### gsc-8d6 — Detect detached HEAD state at startup
- Added `git symbolic-ref --quiet HEAD` check at the top of `check_git_state()`
- Exits with clear error + `git checkout -b <branch>` tip if HEAD is detached
- Committed as `fix: detect detached HEAD state and abort early (gsc-8d6)`

### gsc-zd7 — Extract COMMIT_FORMAT_RULES constant
- Extracted the conventional commit format block (type defs, priority rules, body rules,
  BREAKING_CHANGE detection) into `COMMIT_FORMAT_RULES = textwrap.dedent(...) + BREAKING_CHANGE_RULES`
- Both `SYSTEM_PROMPT` and `AGENTIC_SYSTEM_PROMPT` now use `{COMMIT_FORMAT_RULES}`
  replacing the verbatim-duplicate block
- `MERGE_PROMPT` updated to reference the same type hierarchy with consistent wording
- Parallel work: another worktree had extracted `HUNK_GROUPING_RULES` at the same time;
  merge conflict resolved by combining both: `{HUNK_GROUPING_RULES}\n{COMMIT_FORMAT_RULES}`
- Committed as `refactor: extract COMMIT_FORMAT_RULES constant (gsc-zd7)`

### gsc-cem — Unit tests for pure functions
- Created `tests/test_core.py` covering `split_hunks`, `_parse_hunk_oldrange`, `_is_binary_diff`
- 18 tests, all passing with `uv run --group dev pytest`
- Loader extracts each function via regex so tests run without httpx/pydantic/textual
- Added `pytest>=8.0` to `[dependency-groups] dev` in `pyproject.toml`
- Committed by user as `feat: add tests` (fa8eb80)

## Key Technical Notes

### Prompt constant hierarchy (current state)
```
BREAKING_CHANGE_RULES       — issue detection rules + breaking-change detection
CODE_ISSUE_DETECTION        — code-smell examples (Java/C/Python)  [gsc-952]
HUNK_GROUPING_RULES         — hunk ID format, split rules, cohesion rules  [another WT]
COMMIT_FORMAT_RULES         — conventional commit format + type defs + BREAKING_CHANGE_RULES
```
Both system prompts interpolate all four; `MERGE_PROMPT` uses only type-priority language.

Remaining extraction work (drives gsc-1bd, gsc-dns, gsc-c3z):
- The opening "You are a senior..." sentence is still duplicated
- The junk-file paragraph is still duplicated
- `MERGE_PROMPT` harmonization (gsc-dns) is now unblocked

### Test infrastructure
- Test file: `tests/test_core.py`
- Pure functions extracted via `_extract_function(src, name)` regex — no heavyweight imports
- `pyproject.toml` has `pytest>=8.0` in dev dependencies
- Remaining candidates to add tests for: `merge_overlapping_commits` (needs mock LLM),
  `_build_combined_patch`, `_warn_dependent_splits`, `collect_hunks` (needs temp git repo)

### Worktree workflow reminder
- The shell CWD resets to the worktree after `bd` commands
- Don't use `git -C .claude/worktrees/...` from the worktree — run `git` directly
- Don't use `cd /path/to/main-repo` — stay in the worktree
- Merging worktree → main: run from main repo (`git merge --no-ff worktree-branch`)
  then push both remotes, `bd close <id> --reason="Implemented in <sha>"`

## Next Ready Issues (as of session end)
- gsc-krf (P2): Hunk independence check before commit execution — auto-merge dependent splits
- gsc-71h (P3): --show-config flag + config validation + Pydantic TOML model
- gsc-ftg (P2): Async context compaction
- gsc-1bd (P2): Compose SYSTEM_PROMPT from shared parts (unblocked by gsc-zd7)
- gsc-dns (P2): Harmonize MERGE_PROMPT with shared constants (unblocked by gsc-zd7)
